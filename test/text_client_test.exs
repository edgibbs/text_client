defmodule TextClientTest do
  use ExUnit.Case

  # test "can setup intial state starting the game" do
  #   state = TextClient.start()
  #   assert state.game_service.game_state == :initializing
  #   assert state.tally.turns_left == 7
  #   assert state.guessed == ""
  # end
end
